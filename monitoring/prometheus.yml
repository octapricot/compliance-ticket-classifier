# Prometheus scrape configuration
global:
  scrape_interval: 5s          # scrape targets every 5 seconds

scrape_configs:
  - job_name: "compliance-classifier"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:8000"]   # reaches the FastAPI service on the Mac host
